pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                echo 'Running build automation'
                sh 'git clone https://github.com/Ivanouski1/cinema_test.git'
                sh 'ls -la'
            }
        }
    }
}
